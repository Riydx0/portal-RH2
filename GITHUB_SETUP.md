# GitHub Setup & Push Guide

## للعميل - دليل نشر المشروع على GitHub و Cloudron

### الخطوة 1️⃣: تحضير GitHub

#### 1.1 إنشاء Repository على GitHub
1. اذهب إلى: https://github.com/new
2. أسم المشروع: `portal-RH2` (أو أي اسم تفضله)
3. اختر: **Private** (خاص)
4. انقر: **Create Repository**

---

### الخطوة 2️⃣: نسخ الملفات إلى جهازك

```bash
# اذهب إلى مجلد المشروع
cd /path/to/your/project
```

---

### الخطوة 3️⃣: ربط GitHub

#### 3.1 إذا كان المشروع جديد:

```bash
# تهيئة Git
git init
git add .
git commit -m "Initial commit: IT Service Portal v0.1.0"
git branch -M main

# ربط الـ Repository (استبدل YOUR-USERNAME بـ اسم حسابك)
git remote add origin https://github.com/YOUR-USERNAME/portal-RH2.git

# Push إلى GitHub
git push -u origin main
```

#### 3.2 إذا كان المشروع موجود بالفعل:

```bash
git add .
git commit -m "Update: Add Cloudron deployment files"
git push origin main
```

---

### الخطوة 4️⃣: التحقق من GitHub

1. اذهب إلى: https://github.com/YOUR-USERNAME/portal-RH2
2. يجب أن ترى جميع الملفات 👍

---

### الخطوة 5️⃣: النسخ Cloudron

بعد Push إلى GitHub، استخدم الملف `DEPLOYMENT.md` لتثبيت التطبيق على Cloudron.

---

## 🔧 بعض الأوامر المهمة:

| الأمر | الوصف |
|------|-------|
| `git status` | إظهار الملفات المتغيرة |
| `git add .` | إضافة جميع الملفات |
| `git commit -m "رسالة"` | حفظ التغييرات |
| `git push` | رفع إلى GitHub |
| `git pull` | تحميل آخر نسخة |
| `git log --oneline` | رؤية السجل |

---

## ⚠️ ملاحظات مهمة:

1. **لا تنسَ تغيير بيانات Admin:**
   - بعد التثبيت على Cloudron، غيّر كلمة المرور
   - البيانات الافتراضية: `admin@portal` / `admin`

2. **متغيرات البيئة:**
   - جاهزة في `cloudron.json`
   - اضبطها في لوحة تحكم Cloudron

3. **النسخ الاحتياطية:**
   - Cloudron يعمل النسخ الاحتياطية تلقائياً
   - تأكد من فحص إعدادات النسخ الاحتياطية

---

## 📞 للمساعدة:

- **توثيق Cloudron:** https://docs.cloudron.io
- **المشروع:** https://github.com/Riydx0/portal-RH2
- **ملف التثبيت:** اقرأ `DEPLOYMENT.md`

---

**جاهز للانطلاق!** 🚀
