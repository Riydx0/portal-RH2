المطلوب: بناء نظام IT Portal كامل مع Backend + Database

أريد منك بناء نظام كامل لإدارة خدمات تقنية المعلومات (IT Service Portal) بحيث أستطيع لاحقًا رفعه على Cloudron وتشغيله كسيرفر Web.

🎯 الهدف من النظام

النظام عبارة عن منصة داخلية/للعملاء تحتوي على:

لوحة تحكم Dashboard احترافية

إدارة برامج التحميل (Office، Kaspersky، المتصفحات، البرامج الخدمية…)

إدارة تراخيص/Keys

نظام بسيط لتذاكر الدعم الفني

إدارة العملاء/الأجهزة إن أمكن

نظام دخول (Authentication) وصلاحيات (Roles)

1) التقنية المطلوبة (Tech Stack)

أريد أن تبني المشروع بالتقنيات التالية:

Backend

Node.js + Express

استخدام TypeScript إن كان ممكنًا، وإذا معقّد لا بأس بـ JavaScript عادي.

استخدام REST API منظمة تحت /api/...

Database

استخدام SQLite كقاعدة بيانات (ملف واحد داخل المشروع)، مع إمكانية استبدالها لاحقاً بـ PostgreSQL.

استخدام ORM مثل Prisma أو Knex (يفضل Prisma إذا سهل).

Frontend

واجهة Web احترافية:

HTML + CSS + JavaScript

أو استخدام React + Vite لو ذلك أسهل لك

تصميم Dark Theme حديث يشبه لوحات التحكم (Dashboards) المعروفة.

استخدام TailwindCSS أو Bootstrap (اختيارك).

تشغيل المشروع

المشروع يشتغل بأمر مثلاً:

npm install
npm run dev


أو

npm start


يكون السيرفر يعمل على PORT من الـ environment (مثل process.env.PORT || 3000).

2) أنواع المستخدمين (User Roles)

أريد على الأقل نوعين من المستخدمين:

Admin

إدارة البرامج والفئات

إدارة المستخدمين

إدارة تذاكر الدعم الفني

إدارة التراخيص/Keys

Technician / Support

مشاهدة البرامج وقوائمها

فتح/إغلاق تذاكر دعم

تسجيل ملاحظات على التذكرة

تحميل البرامج

(إن أحببت يمكن إضافة Role ثالث: Viewer / Client)

نظام الدخول (Auth)

تسجيل الدخول بـ email + password

استخدام Sessions أو JWT (اختيارك، لكن المهم يكون آمن وبسيط)

صفحة /login + /logout

حماية صفحات الـ Dashboard بحيث لا يدخلها إلا المستخدمين المسجلين.

3) جداول قاعدة البيانات (Database Models)
3.1 جدول المستخدمين users

id (primary key)

name

email (unique)

password_hash

role (admin / tech / client)

created_at

updated_at

3.2 جدول فئات البرامج categories

id

name (مثلاً: Office, Security, Browsers, Utilities)

description

created_at

3.3 جدول البرامج software

id

name (مثلاً: Microsoft Office 365, Kaspersky Internet Security)

category_id (FK → categories)

description

download_url (رابط تحميل خارجي أو داخلي)

version

platform (Windows / Mac / Both)

is_active (boolean)

created_at

updated_at

3.4 جدول التراخيص/المفاتيح licenses

id

software_id (FK → software)

license_key

assigned_to (اسم العميل أو الجهاز)

notes

status (available / in-use / expired)

created_at

updated_at

3.5 جدول التذاكر tickets

id

title

description

status (open / in-progress / closed)

priority (low / normal / high)

created_by (FK → users)

assigned_to (FK → users, يمكن أن تكون null)

created_at

updated_at

3.6 جدول تعليقات التذاكر ticket_comments

id

ticket_id (FK → tickets)

user_id (FK → users)

comment

created_at

إذا ترى ضرورة لإضافة جداول أخرى مثل clients أو devices يمكنك إضافتها بما يناسب تصميمك.

4) واجهة المستخدم (UI Features)
4.1 صفحة تسجيل الدخول /login

فورم بسيط (email + password)

عند النجاح يوجه المستخدم إلى /dashboard

4.2 Dashboard رئيسية /dashboard

تحتوي على عناصر مثل:

عدد البرامج المسجلة

عدد التذاكر المفتوحة

عدد التذاكر المغلقة

عدد التراخيص المتاحة / المستخدمة

إحصائيات بسيطة (Cards / Charts اختياري)

4.3 صفحة إدارة البرامج /admin/software

جدول يعرض:

اسم البرنامج

الفئة

الإصدار

الحالة (فعال/غير فعال)

أزرار:

إضافة برنامج جديد

تعديل

حذف

4.4 صفحة إضافة/تعديل برنامج

فورم يحتوي:

الاسم

الفئة (قائمة منسدلة)

الوصف

رابط التحميل

الإصدار

نوع النظام (Windows / Mac / Both)

حالة البرنامج

4.5 واجهة عرض البرامج للتحميل /downloads

قائمة منظمة بالبرامج حسب الفئة

إمكانية البحث عن برنامج

زر “تحميل” لكل برنامج

4.6 نظام التذاكر /tickets

صفحة عرض التذاكر (قابلة للفلترة حسب الحالة)

صفحة تفاصيل التذكرة فيها:

معلومات التذكرة

تعليقات/ملاحظات الفنيين

زر تغيير الحالة (open / in-progress / closed)

صفحة فتح تذكرة جديدة (title + description + priority)

4.7 إدارة التراخيص /licenses

قائمة بالتراخيص المرتبطة بكل برنامج

إمكانية:

إضافة License جديد

تعديل/تغيير حالته (available/in-use/expired)

ربطه بعميل أو جهاز

5) واجهة الـ Admin

قائمة جانبية (Sidebar) فيها:

Dashboard

Software

Categories

Licenses

Tickets

Users (إن أمكن)

التصميم Dark Theme احترافي مع Cards و Tables نظيفة.

6) متطلبات أخرى

إضافة Seeder/Script لإنشاء:

مستخدم Admin افتراضي (مثلاً: email: admin@example.com, password: Admin123!)

بعض الفئات (Office, Security, Browsers, Utilities)

بعض البرامج التجريبية

توثيق بسيط في ملف README.md يشرح:

كيفية تشغيل المشروع محليًا

كيفية تغيير بيانات الدخول الافتراضية

مكان ملف قاعدة البيانات SQLite

المطلوب منك: تنفيذ النظام بالكامل (Backend + Database + Frontend Dashboard) وفق ما سبق، مع تنظيم الكود في مجلدات واضحة (routes, controllers, models, views أو components إن استخدمت React).