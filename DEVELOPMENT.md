# دليل التطوير والمساهمة

## 🎯 كيفية العمل مع المشروع

### 1. البيئة المحلية (Development)

```bash
# تثبيت الـ dependencies
npm install

# تشغيل في وضع التطوير
npm run dev

# فتح المتصفح على
http://localhost:5000
```

### 2. هيكل الـ Branches

نستخدم نموذج Git Flow البسيط:

- **`main`** - الإصدار المستقر (Production)
- **`feature/xxx`** - ميزات جديدة
- **`bugfix/xxx`** - إصلاح الأخطاء
- **`hotfix/xxx`** - إصلاحات سريعة

### 3. خطوات التطوير

#### الخطوة 1: إنشاء branch جديد
```bash
# تحديث من main أولاً
git pull origin main

# إنشاء branch للتطوير
git checkout -b feature/new-feature
```

#### الخطوة 2: كتابة الكود
- اتبع أسلوب الكود الموجود
- أضف تعليقات واضحة
- اختبر التغييرات محلياً

#### الخطوة 3: الحفظ (Commit)
```bash
# إضافة الملفات
git add .

# كتابة رسالة واضحة
git commit -m "feat: إضافة ميزة X"
git commit -m "fix: إصلاح مشكلة Y"
git commit -m "docs: تحديث التوثيق"
```

#### الخطوة 4: الرفع على GitHub
```bash
# رفع branch
git push origin feature/new-feature

# إنشاء Pull Request على GitHub
# اختر main كـ base branch
```

#### الخطوة 5: الـ Merge إلى main
```bash
# بعد الموافقة على PR
git checkout main
git pull origin main
git merge feature/new-feature
git push origin main
```

### 4. رسائل الـ Commit

استخدم الصيغة التالية:

```
feat: إضافة ميزة جديدة           # ميزة جديدة
fix: إصلاح خطأ في الكود          # إصلاح خطأ
docs: تحديث الوثائق              # وثائق
style: تحسين التنسيق             # تنسيق الكود
refactor: تحسين الهيكل           # إعادة بناء
perf: تحسين الأداء               # أداء
test: إضافة اختبارات             # اختبارات
chore: تحديث المكتبات            # مهام إدارية
```

## 🧪 الاختبار

### تشغيل الاختبارات
```bash
npm run check    # فحص TypeScript
npm run build    # بناء الإصدار الإنتاجي
```

### اختبار الكود يدويا
1. شغّل `npm run dev`
2. افتح `http://localhost:5000`
3. اختبر الميزات الجديدة
4. تأكد من عدم وجود أخطاء في console

## 📦 إنشاء إصدار جديد

عند الانتهاء من مجموعة من الميزات:

1. **تحديث رقم النسخة** في `VERSION`:
   ```
   0.1.0 → 0.2.0  (تحسينات رئيسية)
   0.1.0 → 0.1.1  (إصلاحات بسيطة)
   ```

2. **إنشاء Release على GitHub** مع الوصف

3. **نشر على Cloudron** (سيتم تلقائياً)

## 🔒 الأمان

- ❌ لا تكتب كلمات المرور في الكود
- ❌ لا تنسخ ملف `.env`
- ✅ استخدم environment variables
- ✅ تحقق من مدخلات المستخدم
- ✅ استخدم HTTPS في الإنتاج

## 📱 المتطلبات قبل الـ Push

قائمة التحقق:

- [ ] الكود يعمل محلياً بدون أخطاء
- [ ] لا توجد أخطاء TypeScript (`npm run check`)
- [ ] الملفات الحساسة في `.gitignore`
- [ ] رسالة الـ commit واضحة
- [ ] لا توجد ملفات debug أو test ترفع
- [ ] اختبرت الميزة على جهازك

## 🆘 حل المشاكل الشائعة

### خطأ: "Database connection refused"
```bash
# تأكد من أن PostgreSQL يعمل
# وأن DATABASE_URL صحيح في .env
```

### خطأ: "Node modules مفقودة"
```bash
npm install
```

### عكس آخر commit
```bash
git revert HEAD
git push origin main
```

### حذف branch محلي
```bash
git branch -d feature/old-feature
```

## 📞 التواصل

- أسئلة عن التطوير؟ ادفع issue على GitHub
- وجدت خطأ؟ أنشئ bug report

---

**آخر تحديث:** November 26, 2025
